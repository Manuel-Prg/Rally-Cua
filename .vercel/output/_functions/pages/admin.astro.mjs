import { e as createComponent, r as renderTemplate, k as defineScriptVars, l as renderComponent, m as maybeRenderHead, g as addAttribute } from '../chunks/astro/server_CwtUBU6d.mjs';
import 'piccolore';
import { P as PHASES, $ as $$BaseLayout } from '../chunks/mockData_B_BV1TJC.mjs';
import { s as supabase } from '../chunks/supabase_BwOqVcUI.mjs';
/* empty css                                 */
export { renderers } from '../renderers.mjs';

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _a;
const $$Admin = createComponent(async ($$result, $$props, $$slots) => {
  const { data: rawTeams, error } = await supabase.from("teams").select("id, name, color, members, scores(phase_id, status, points)").order("id");
  if (error) console.error("Admin fetch error:", error);
  const teams = (rawTeams ?? []).map((row) => ({
    id: row.id,
    name: row.name,
    color: row.color,
    members: Array.isArray(row.members) ? row.members : [],
    phases: [1, 2, 3, 4, 5].map((phaseId) => {
      const s = (row.scores ?? []).find((sc) => sc.phase_id === phaseId);
      return { phaseId, status: s?.status ?? "pending", points: s?.points ?? 0 };
    })
  }));
  const teamsJson = JSON.stringify(teams);
  return renderTemplate(_a || (_a = __template(["", " <script>(function(){", '\n  // \u2500\u2500 State from Supabase SSR \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n  let teams = JSON.parse(teamsJson);\n  let activePhaseFilter = "all";\n\n  function totalScore(team) {\n    return team.phases.reduce((sum, p) => sum + p.points, 0);\n  }\n\n  // \u2500\u2500 Helpers \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n  function getDisplayPhaseIdx(team) {\n    if (activePhaseFilter !== "all") return activePhaseFilter - 1;\n    const doingIdx = team.phases.findIndex((p) => p.status === "doing");\n    if (doingIdx >= 0) return doingIdx;\n    const lastDone = [...team.phases]\n      .map((p, i) => ({ ...p, i }))\n      .filter((p) => p.status === "done")\n      .at(-1);\n    return lastDone ? lastDone.i : 0;\n  }\n\n  // \u2500\u2500 Render table \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n  function renderTable() {\n    const tbody = document.getElementById("admin-tbody");\n    const thPhase = document.getElementById("th-phase");\n\n    thPhase.textContent =\n      activePhaseFilter === "all"\n        ? "Fase activa"\n        : PHASES[activePhaseFilter - 1].name;\n\n    if (teams.length === 0) {\n      tbody.innerHTML =\n        \'<tr><td colspan="6" style="text-align:center;padding:2rem;opacity:0.5;font-family:Barlow Condensed,sans-serif;letter-spacing:0.1em">No hay equipos registrados en Supabase.</td></tr>\';\n      return;\n    }\n\n    tbody.innerHTML = teams\n      .map((team) => {\n        const pi = getDisplayPhaseIdx(team);\n        const phase = team.phases[pi];\n        const pname = PHASES[pi].name;\n        const total = totalScore(team);\n\n        return `\n    <tr data-team="${team.id}">\n      <td>\n        <div class="team-cell">\n          <span class="team-dot" style="background:${team.color}"></span>\n          <span class="team-cell-name">${team.name}</span>\n        </div>\n      </td>\n      <td class="phase-name-cell">${pname}</td>\n      <td>\n        <select class="status-select" data-team="${team.id}" data-phase="${phase.phaseId}">\n          <option value="pending" ${phase.status === "pending" ? "selected" : ""}>Pendiente</option>\n          <option value="doing"   ${phase.status === "doing" ? "selected" : ""}>En curso</option>\n          <option value="done"    ${phase.status === "done" ? "selected" : ""}>Completado</option>\n        </select>\n      </td>\n      <td>\n        <input\n          class="pts-input"\n          type="number" min="0" max="999"\n          value="${phase.points}"\n          data-team="${team.id}"\n          data-phase="${phase.phaseId}"\n        />\n      </td>\n      <td>\n        <span class="total-display" id="total-${team.id}">${total}</span>\n      </td>\n      <td>\n        <button class="btn btn-save btn-sm save-btn" data-team="${team.id}">Guardar</button>\n      </td>\n    </tr>`;\n      })\n      .join("");\n\n    attachTableListeners();\n  }\n\n  function attachTableListeners() {\n    // Points input \u2192 update running total live\n    document.querySelectorAll(".pts-input").forEach((input) => {\n      input.addEventListener("input", () => {\n        const tid = Number(input.dataset.team);\n        const pid = Number(input.dataset.phase);\n        const team = teams.find((t) => t.id === tid);\n        team.phases.find((p) => p.phaseId === pid).points =\n          Number(input.value) || 0;\n        const el = document.getElementById(`total-${tid}`);\n        if (el) el.textContent = String(totalScore(team));\n      });\n    });\n\n    // Status select\n    document.querySelectorAll(".status-select").forEach((sel) => {\n      sel.addEventListener("change", () => {\n        const tid = Number(sel.dataset.team);\n        const pid = Number(sel.dataset.phase);\n        const team = teams.find((t) => t.id === tid);\n        team.phases.find((p) => p.phaseId === pid).status = sel.value;\n      });\n    });\n\n    // Save button \u2192 call /api/update-score\n    document.querySelectorAll(".save-btn").forEach((btn) => {\n      btn.addEventListener("click", async () => {\n        const tid = Number(btn.dataset.team);\n        const team = teams.find((t) => t.id === tid);\n        const pi = getDisplayPhaseIdx(team);\n        const phase = team.phases[pi];\n\n        btn.textContent = "\u2026";\n        btn.disabled = true;\n\n        try {\n          const res = await fetch("/api/update-score", {\n            method: "POST",\n            headers: { "Content-Type": "application/json" },\n            body: JSON.stringify({\n              team_id: tid,\n              id_fase: phase.phaseId,\n              estatus: phase.status,\n              puntos: phase.points,\n            }),\n          });\n          const data = await res.json();\n          if (!res.ok) {\n            showToast("Error: " + (data.error ?? "Intenta de nuevo"), "error");\n          } else {\n            btn.textContent = "\u2713 Guardado";\n            btn.classList.add("saved");\n            showToast(`${team.name} \u2014 Fase ${phase.phaseId} guardada`);\n            setTimeout(() => {\n              btn.textContent = "Guardar";\n              btn.classList.remove("saved");\n              btn.disabled = false;\n            }, 2000);\n            return;\n          }\n        } catch {\n          showToast("Error de red. Intenta de nuevo.", "error");\n        }\n\n        btn.textContent = "Guardar";\n        btn.disabled = false;\n      });\n    });\n  }\n\n  // \u2500\u2500 Phase filter \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n  document.getElementById("phase-filter").addEventListener("click", (e) => {\n    const target = e.target;\n    if (!target.classList.contains("phase-btn")) return;\n    document\n      .querySelectorAll(".phase-btn")\n      .forEach((b) => b.classList.remove("active"));\n    target.classList.add("active");\n    activePhaseFilter =\n      target.dataset.phase === "all" ? "all" : Number(target.dataset.phase);\n    renderTable();\n  });\n\n  // \u2500\u2500 Register modal \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n  const modal = document.getElementById("modal-register");\n\n  document.getElementById("btn-register").addEventListener("click", () => {\n    modal.classList.add("open");\n    document.getElementById("input-name").focus();\n  });\n\n  document\n    .getElementById("btn-modal-cancel")\n    .addEventListener("click", () => modal.classList.remove("open"));\n  modal.addEventListener("click", (e) => {\n    if (e.target === modal) modal.classList.remove("open");\n  });\n\n  document\n    .getElementById("btn-modal-save")\n    .addEventListener("click", async () => {\n      const nameEl = document.getElementById("input-name");\n      const membersEl = document.getElementById("input-members");\n      const colorEl = document.getElementById("input-color");\n\n      const name = nameEl.value.trim();\n      if (!name) {\n        nameEl.focus();\n        return;\n      }\n\n      const members = membersEl.value\n        .split(",")\n        .map((m) => m.trim())\n        .filter(Boolean);\n      const saveBtn = document.getElementById("btn-modal-save");\n      saveBtn.textContent = "Guardando\u2026";\n      saveBtn.disabled = true;\n\n      try {\n        const res = await fetch("/api/create-team", {\n          method: "POST",\n          headers: { "Content-Type": "application/json" },\n          body: JSON.stringify({\n            nombre: name,\n            color: colorEl.value,\n            miembros: members,\n          }),\n        });\n        const data = await res.json();\n        if (!res.ok) {\n          showToast("Error: " + (data.error ?? "Intenta de nuevo"), "error");\n        } else {\n          // Add to local state\n          teams.push({\n            id: data.id,\n            name,\n            color: colorEl.value,\n            members,\n            phases: [1, 2, 3, 4, 5].map((id) => ({\n              phaseId: id,\n              status: "pending",\n              points: 0,\n            })),\n          });\n          modal.classList.remove("open");\n          nameEl.value = "";\n          membersEl.value = "";\n          renderTable();\n          showToast(`Equipo "${name}" registrado`);\n        }\n      } catch {\n        showToast("Error de red.", "error");\n      }\n\n      saveBtn.textContent = "Registrar";\n      saveBtn.disabled = false;\n    });\n\n  // \u2500\u2500 Export Excel \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n  document.getElementById("btn-export").addEventListener("click", async () => {\n    const XLSX =\n      await import("https://cdn.sheetjs.com/xlsx-0.20.2/package/xlsx.mjs");\n    const wb = XLSX.utils.book_new();\n\n    const sorted = [...teams].sort((a, b) => totalScore(b) - totalScore(a));\n    const rows1 = [\n      ["Pos", "Equipo", "Miembros", ...PHASES.map((p) => p.name), "Total"],\n    ];\n    sorted.forEach((t, i) =>\n      rows1.push([\n        i + 1,\n        t.name,\n        t.members.join(", "),\n        ...t.phases.map((p) => p.points),\n        totalScore(t),\n      ]),\n    );\n    XLSX.utils.book_append_sheet(\n      wb,\n      XLSX.utils.aoa_to_sheet(rows1),\n      "Clasificaci\xF3n",\n    );\n\n    const rows2 = [["Equipo", "Fase", "Estado", "Puntos"]];\n    teams.forEach((t) =>\n      t.phases.forEach((p, i) =>\n        rows2.push([t.name, PHASES[i].name, p.status, p.points]),\n      ),\n    );\n    XLSX.utils.book_append_sheet(\n      wb,\n      XLSX.utils.aoa_to_sheet(rows2),\n      "Detalle Fases",\n    );\n\n    XLSX.writeFile(wb, `rally-${new Date().toISOString().slice(0, 10)}.xlsx`);\n    showToast("Excel exportado");\n  });\n\n  // \u2500\u2500 Toast \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n  function showToast(msg, type = "success") {\n    const area = document.getElementById("toast-area");\n    const t = document.createElement("div");\n    t.className = "toast" + (type === "error" ? " toast-error" : "");\n    t.textContent = (type === "error" ? "\u26A0 " : "\u2713 ") + msg;\n    area.appendChild(t);\n    setTimeout(() => t.classList.add("toast-out"), 2600);\n    setTimeout(() => t.remove(), 3000);\n  }\n\n  // \u2500\u2500 Init \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n  renderTable();\n})();<\/script> '], ["", " <script>(function(){", '\n  // \u2500\u2500 State from Supabase SSR \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n  let teams = JSON.parse(teamsJson);\n  let activePhaseFilter = "all";\n\n  function totalScore(team) {\n    return team.phases.reduce((sum, p) => sum + p.points, 0);\n  }\n\n  // \u2500\u2500 Helpers \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n  function getDisplayPhaseIdx(team) {\n    if (activePhaseFilter !== "all") return activePhaseFilter - 1;\n    const doingIdx = team.phases.findIndex((p) => p.status === "doing");\n    if (doingIdx >= 0) return doingIdx;\n    const lastDone = [...team.phases]\n      .map((p, i) => ({ ...p, i }))\n      .filter((p) => p.status === "done")\n      .at(-1);\n    return lastDone ? lastDone.i : 0;\n  }\n\n  // \u2500\u2500 Render table \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n  function renderTable() {\n    const tbody = document.getElementById("admin-tbody");\n    const thPhase = document.getElementById("th-phase");\n\n    thPhase.textContent =\n      activePhaseFilter === "all"\n        ? "Fase activa"\n        : PHASES[activePhaseFilter - 1].name;\n\n    if (teams.length === 0) {\n      tbody.innerHTML =\n        \'<tr><td colspan="6" style="text-align:center;padding:2rem;opacity:0.5;font-family:Barlow Condensed,sans-serif;letter-spacing:0.1em">No hay equipos registrados en Supabase.</td></tr>\';\n      return;\n    }\n\n    tbody.innerHTML = teams\n      .map((team) => {\n        const pi = getDisplayPhaseIdx(team);\n        const phase = team.phases[pi];\n        const pname = PHASES[pi].name;\n        const total = totalScore(team);\n\n        return \\`\n    <tr data-team="\\${team.id}">\n      <td>\n        <div class="team-cell">\n          <span class="team-dot" style="background:\\${team.color}"></span>\n          <span class="team-cell-name">\\${team.name}</span>\n        </div>\n      </td>\n      <td class="phase-name-cell">\\${pname}</td>\n      <td>\n        <select class="status-select" data-team="\\${team.id}" data-phase="\\${phase.phaseId}">\n          <option value="pending" \\${phase.status === "pending" ? "selected" : ""}>Pendiente</option>\n          <option value="doing"   \\${phase.status === "doing" ? "selected" : ""}>En curso</option>\n          <option value="done"    \\${phase.status === "done" ? "selected" : ""}>Completado</option>\n        </select>\n      </td>\n      <td>\n        <input\n          class="pts-input"\n          type="number" min="0" max="999"\n          value="\\${phase.points}"\n          data-team="\\${team.id}"\n          data-phase="\\${phase.phaseId}"\n        />\n      </td>\n      <td>\n        <span class="total-display" id="total-\\${team.id}">\\${total}</span>\n      </td>\n      <td>\n        <button class="btn btn-save btn-sm save-btn" data-team="\\${team.id}">Guardar</button>\n      </td>\n    </tr>\\`;\n      })\n      .join("");\n\n    attachTableListeners();\n  }\n\n  function attachTableListeners() {\n    // Points input \u2192 update running total live\n    document.querySelectorAll(".pts-input").forEach((input) => {\n      input.addEventListener("input", () => {\n        const tid = Number(input.dataset.team);\n        const pid = Number(input.dataset.phase);\n        const team = teams.find((t) => t.id === tid);\n        team.phases.find((p) => p.phaseId === pid).points =\n          Number(input.value) || 0;\n        const el = document.getElementById(\\`total-\\${tid}\\`);\n        if (el) el.textContent = String(totalScore(team));\n      });\n    });\n\n    // Status select\n    document.querySelectorAll(".status-select").forEach((sel) => {\n      sel.addEventListener("change", () => {\n        const tid = Number(sel.dataset.team);\n        const pid = Number(sel.dataset.phase);\n        const team = teams.find((t) => t.id === tid);\n        team.phases.find((p) => p.phaseId === pid).status = sel.value;\n      });\n    });\n\n    // Save button \u2192 call /api/update-score\n    document.querySelectorAll(".save-btn").forEach((btn) => {\n      btn.addEventListener("click", async () => {\n        const tid = Number(btn.dataset.team);\n        const team = teams.find((t) => t.id === tid);\n        const pi = getDisplayPhaseIdx(team);\n        const phase = team.phases[pi];\n\n        btn.textContent = "\u2026";\n        btn.disabled = true;\n\n        try {\n          const res = await fetch("/api/update-score", {\n            method: "POST",\n            headers: { "Content-Type": "application/json" },\n            body: JSON.stringify({\n              team_id: tid,\n              id_fase: phase.phaseId,\n              estatus: phase.status,\n              puntos: phase.points,\n            }),\n          });\n          const data = await res.json();\n          if (!res.ok) {\n            showToast("Error: " + (data.error ?? "Intenta de nuevo"), "error");\n          } else {\n            btn.textContent = "\u2713 Guardado";\n            btn.classList.add("saved");\n            showToast(\\`\\${team.name} \u2014 Fase \\${phase.phaseId} guardada\\`);\n            setTimeout(() => {\n              btn.textContent = "Guardar";\n              btn.classList.remove("saved");\n              btn.disabled = false;\n            }, 2000);\n            return;\n          }\n        } catch {\n          showToast("Error de red. Intenta de nuevo.", "error");\n        }\n\n        btn.textContent = "Guardar";\n        btn.disabled = false;\n      });\n    });\n  }\n\n  // \u2500\u2500 Phase filter \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n  document.getElementById("phase-filter").addEventListener("click", (e) => {\n    const target = e.target;\n    if (!target.classList.contains("phase-btn")) return;\n    document\n      .querySelectorAll(".phase-btn")\n      .forEach((b) => b.classList.remove("active"));\n    target.classList.add("active");\n    activePhaseFilter =\n      target.dataset.phase === "all" ? "all" : Number(target.dataset.phase);\n    renderTable();\n  });\n\n  // \u2500\u2500 Register modal \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n  const modal = document.getElementById("modal-register");\n\n  document.getElementById("btn-register").addEventListener("click", () => {\n    modal.classList.add("open");\n    document.getElementById("input-name").focus();\n  });\n\n  document\n    .getElementById("btn-modal-cancel")\n    .addEventListener("click", () => modal.classList.remove("open"));\n  modal.addEventListener("click", (e) => {\n    if (e.target === modal) modal.classList.remove("open");\n  });\n\n  document\n    .getElementById("btn-modal-save")\n    .addEventListener("click", async () => {\n      const nameEl = document.getElementById("input-name");\n      const membersEl = document.getElementById("input-members");\n      const colorEl = document.getElementById("input-color");\n\n      const name = nameEl.value.trim();\n      if (!name) {\n        nameEl.focus();\n        return;\n      }\n\n      const members = membersEl.value\n        .split(",")\n        .map((m) => m.trim())\n        .filter(Boolean);\n      const saveBtn = document.getElementById("btn-modal-save");\n      saveBtn.textContent = "Guardando\u2026";\n      saveBtn.disabled = true;\n\n      try {\n        const res = await fetch("/api/create-team", {\n          method: "POST",\n          headers: { "Content-Type": "application/json" },\n          body: JSON.stringify({\n            nombre: name,\n            color: colorEl.value,\n            miembros: members,\n          }),\n        });\n        const data = await res.json();\n        if (!res.ok) {\n          showToast("Error: " + (data.error ?? "Intenta de nuevo"), "error");\n        } else {\n          // Add to local state\n          teams.push({\n            id: data.id,\n            name,\n            color: colorEl.value,\n            members,\n            phases: [1, 2, 3, 4, 5].map((id) => ({\n              phaseId: id,\n              status: "pending",\n              points: 0,\n            })),\n          });\n          modal.classList.remove("open");\n          nameEl.value = "";\n          membersEl.value = "";\n          renderTable();\n          showToast(\\`Equipo "\\${name}" registrado\\`);\n        }\n      } catch {\n        showToast("Error de red.", "error");\n      }\n\n      saveBtn.textContent = "Registrar";\n      saveBtn.disabled = false;\n    });\n\n  // \u2500\u2500 Export Excel \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n  document.getElementById("btn-export").addEventListener("click", async () => {\n    const XLSX =\n      await import("https://cdn.sheetjs.com/xlsx-0.20.2/package/xlsx.mjs");\n    const wb = XLSX.utils.book_new();\n\n    const sorted = [...teams].sort((a, b) => totalScore(b) - totalScore(a));\n    const rows1 = [\n      ["Pos", "Equipo", "Miembros", ...PHASES.map((p) => p.name), "Total"],\n    ];\n    sorted.forEach((t, i) =>\n      rows1.push([\n        i + 1,\n        t.name,\n        t.members.join(", "),\n        ...t.phases.map((p) => p.points),\n        totalScore(t),\n      ]),\n    );\n    XLSX.utils.book_append_sheet(\n      wb,\n      XLSX.utils.aoa_to_sheet(rows1),\n      "Clasificaci\xF3n",\n    );\n\n    const rows2 = [["Equipo", "Fase", "Estado", "Puntos"]];\n    teams.forEach((t) =>\n      t.phases.forEach((p, i) =>\n        rows2.push([t.name, PHASES[i].name, p.status, p.points]),\n      ),\n    );\n    XLSX.utils.book_append_sheet(\n      wb,\n      XLSX.utils.aoa_to_sheet(rows2),\n      "Detalle Fases",\n    );\n\n    XLSX.writeFile(wb, \\`rally-\\${new Date().toISOString().slice(0, 10)}.xlsx\\`);\n    showToast("Excel exportado");\n  });\n\n  // \u2500\u2500 Toast \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n  function showToast(msg, type = "success") {\n    const area = document.getElementById("toast-area");\n    const t = document.createElement("div");\n    t.className = "toast" + (type === "error" ? " toast-error" : "");\n    t.textContent = (type === "error" ? "\u26A0 " : "\u2713 ") + msg;\n    area.appendChild(t);\n    setTimeout(() => t.classList.add("toast-out"), 2600);\n    setTimeout(() => t.remove(), 3000);\n  }\n\n  // \u2500\u2500 Init \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n  renderTable();\n})();<\/script> '])), renderComponent($$result, "BaseLayout", $$BaseLayout, { "title": "Panel Admin", "activePage": "admin", "data-astro-cid-2zp6q64z": true }, { "default": async ($$result2) => renderTemplate` ${maybeRenderHead()}<div class="admin-view" data-astro-cid-2zp6q64z> <!-- ── HEADER ── --> <header class="admin-header" data-astro-cid-2zp6q64z> <div data-astro-cid-2zp6q64z> <p class="admin-eyebrow" data-astro-cid-2zp6q64z>Panel de control</p> <h1 class="admin-title" data-astro-cid-2zp6q64z>Actualizar Marcador</h1> </div> <div class="header-actions" data-astro-cid-2zp6q64z> <button class="btn btn-export" id="btn-export" data-astro-cid-2zp6q64z> <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" data-astro-cid-2zp6q64z> <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4" data-astro-cid-2zp6q64z></path><polyline points="7 10 12 15 17 10" data-astro-cid-2zp6q64z></polyline><line x1="12" y1="15" x2="12" y2="3" data-astro-cid-2zp6q64z></line> </svg>
Exportar Excel
</button> <button class="btn btn-primary" id="btn-register" data-astro-cid-2zp6q64z>
+ Registrar Equipo
</button> </div> </header> <!-- ── PHASE FILTER ── --> <div class="filter-block" data-astro-cid-2zp6q64z> <span class="filter-label" data-astro-cid-2zp6q64z>Filtrar fase</span> <div class="phase-btns" id="phase-filter" role="group" aria-label="Filtro de fases" data-astro-cid-2zp6q64z> <button class="phase-btn active" data-phase="all" data-astro-cid-2zp6q64z>Todas</button> ${PHASES.map((p) => renderTemplate`<button class="phase-btn"${addAttribute(p.id, "data-phase")} data-astro-cid-2zp6q64z> ${p.name} </button>`)} </div> </div> <!-- ── TABLE ── --> <div class="table-wrapper" data-astro-cid-2zp6q64z> <table class="admin-table" id="admin-table" data-astro-cid-2zp6q64z> <thead data-astro-cid-2zp6q64z> <tr data-astro-cid-2zp6q64z> <th data-astro-cid-2zp6q64z>Equipo</th> <th id="th-phase" data-astro-cid-2zp6q64z>Fase activa</th> <th data-astro-cid-2zp6q64z>Estado</th> <th data-astro-cid-2zp6q64z>Puntos fase</th> <th data-astro-cid-2zp6q64z>Total</th> <th data-astro-cid-2zp6q64z></th> </tr> </thead> <tbody id="admin-tbody" data-astro-cid-2zp6q64z> <!-- Rendered by JS below --> </tbody> </table> </div> <p class="table-note" data-astro-cid-2zp6q64z>
* Los cambios se guardan en Supabase y se reflejan en el marcador público
      al instante.
</p> </div>  <div class="modal-overlay" id="modal-register" aria-modal="true" role="dialog" aria-labelledby="modal-title" data-astro-cid-2zp6q64z> <div class="modal" data-astro-cid-2zp6q64z> <h2 class="modal-title" id="modal-title" data-astro-cid-2zp6q64z>Registrar Equipo</h2> <div class="form-grid" data-astro-cid-2zp6q64z> <div class="form-group" data-astro-cid-2zp6q64z> <label class="form-label" for="input-name" data-astro-cid-2zp6q64z>Nombre del equipo</label> <input class="form-input" id="input-name" type="text" placeholder="Ej. Los Relámpagos" autocomplete="off" data-astro-cid-2zp6q64z> </div> <div class="form-group" data-astro-cid-2zp6q64z> <label class="form-label" for="input-members" data-astro-cid-2zp6q64z>Miembros <small data-astro-cid-2zp6q64z>(separados por coma)</small></label> <input class="form-input" id="input-members" type="text" placeholder="Ana, Carlos, Luis, María, Pedro" data-astro-cid-2zp6q64z> </div> <div class="form-group" data-astro-cid-2zp6q64z> <label class="form-label" for="input-color" data-astro-cid-2zp6q64z>Color del equipo</label> <input class="form-input form-color" id="input-color" type="color" value="#E8531A" data-astro-cid-2zp6q64z> </div> </div> <div class="form-actions" data-astro-cid-2zp6q64z> <button class="btn btn-ghost" id="btn-modal-cancel" data-astro-cid-2zp6q64z>Cancelar</button> <button class="btn btn-primary" id="btn-modal-save" data-astro-cid-2zp6q64z>Registrar</button> </div> </div> </div>  <div class="toast-area" id="toast-area" aria-live="polite" data-astro-cid-2zp6q64z></div> ` }), defineScriptVars({ teamsJson, PHASES }));
}, "C:/Users/Manue/Documents/Rally-Cua/src/pages/admin.astro", void 0);

const $$file = "C:/Users/Manue/Documents/Rally-Cua/src/pages/admin.astro";
const $$url = "/admin";

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
  __proto__: null,
  default: $$Admin,
  file: $$file,
  url: $$url
}, Symbol.toStringTag, { value: 'Module' }));

const page = () => _page;

export { page };
